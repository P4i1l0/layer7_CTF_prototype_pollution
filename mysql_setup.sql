ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '21771126!';
FLUSH PRIVILEGES;
CREATE DATABASE IF NOT EXISTS layer7;
USE layer7;
CREATE TABLE IF NOT EXISTS users(id INT PRIMARY KEY AUTO_INCREMENT, pw VARCHAR(255) NOT NULL);
